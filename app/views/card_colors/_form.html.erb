<%= form_for(@card_color) do |f| %>
  <% if @card_color.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@card_color.errors.count, "error") %> prohibited this card_color from being saved:</h2>

      <ul>
      <% @card_color.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :order %><br />
    <%= f.number_field :order %>
  </div>
  <div class="field">
    <%= f.label :code %><br />
    <%= f.text_field :code %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
