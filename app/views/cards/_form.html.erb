<%= form_for(@card) do |f| %>
  <% if @card.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@card.errors.count, "error") %> prohibited this card from being saved:</h2>

      <ul>
      <% @card.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :word %><br />
    <%= f.text_field :word %>
  </div>
  <div class="field">
    <%= f.label :mean %><br />
    <%= f.text_field :mean %>
  </div>

  <div class="field">
    <%= f.hidden_field :directory_id, value: params[:directory_id] if params[:directory_id].present? %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
